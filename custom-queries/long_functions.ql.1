/**
 * @name Long Functions
 * @description Detects functions in JavaScript that exceed a specified number of lines.
 * @kind problem
 * @problem.severity recommendation
 * @tags maintainability, readability, code-smell
 * @id js/long-functions
 */

import javascript

/**
 * Define a function as long if it exceeds 50 lines.
 */
predicate isLongFunction(Function f) {
  f.getEndLine() - f.getStartLine() > 50
}

from Function f
where isLongFunction(f)
select f, "This function is too long with " + (f.getEndLine() - f.getStartLine()) + " lines."
